@using DirectoryControl.Common;
@{
    ViewBag.Title = "Structure";
}

<h1 class="mb-3">Disk structure</h1>
<div class="row mb-3 container-fluid">
    @foreach (var directory in Helper.CollectNames(Model.Directories))
    {
        <pre class="tree">@directory</pre>
    }
</div>